#import "../../../../lmcc-theme.typ": *

#show: lmcc-theme.with(
  level: "赛项2·识物工坊 Python备赛",
  lesson: "04",
  title: "机器学习与二分类——训练台在做什么",
  date: "2026年",
)

= 机器学习与二分类——训练台在做什么

#objective[
- 分清"分类"和"回归"，理解橙子判断是二分类
- 分清训练集/验证集/测试集
- 理解"样本、特征、标签"、批大小、学习率
- 明白数据健康度检查的意义
- 看懂训练台参数（批大小 16、学习率 0.001）背后的含义
]

#warning[
*本节定位*：你在识物工坊点"开始训练"，后台其实在跑一个机器学习过程。本节把"橙子/非橙子"二分类讲透，让你知道每个参数在干嘛，调参不盲目。
]

== 课前准备（Windows 课堂环境）

- 本讲只用标准库，无需装库
- 课堂脚本：本讲 `课堂脚本/step1` 至 `step3`；step3 是"下山找宝藏"学习率实验，先让学生猜三种步长的结局再跑
- 概念多而代码轻，节奏上多留讨论时间

== 〇、分类 vs 回归（5分钟）

#key-concept("二分类是什么", [
- *分类*：判断"是哪一类"。识物工坊判断一张图是"橙子"还是"非橙子"——*二分类*（只有两类）。
- *回归*：预测一个数值（如房价）。这里用不到。
- 识物工坊：输入一张图 → 输出"橙子 / 非橙子"之一。这就是二分类。
])

== 一、数据集：训练/验证/测试（15分钟）

=== 1.1 三套数据各干什么

#key-concept("三套数据", [
| 数据集 | 用途 | 通俗理解 |
|:----:|:----|:--------|
| 训练集 | 让模型学 | 给模型看题目和答案，让它学规律 |
| 验证集 | 调参时看 | 学的时候小测验，判断学得好不好 |
| 测试集 | 最后检验 | 从未见过的题，考真本事 |

识物工坊训练时内部会分一部分做验证；你单独留的测试图 = 最终的"考试"，绝不进训练。
])

=== 1.2 为什么不能拿测试图训练

#warning[
若把最终测试的图混进训练，模型"见过答案"，自测分会虚高。*测试集必须在训练时完全隔离*——这正是第3讲"8:2划分"防泄漏的原因。
]

== 二、样本·特征·标签（15分钟）

=== 2.1 三个概念

#practice[
*操作*：理解一张橙子图在模型眼里是什么。

+ *样本*：一张图（一个要判断的对象）
+ *特征*：图里提取出的数字信息（颜色、纹理…）
+ *标签*：答案，橙子=1（或"橙子"），非橙子=0（或"非橙子"）

```python
# 用字典表示一个样本
sample = {
    "path": "orange_1.jpg",       # 样本
    "features": [255, 150, 30],   # 特征（此处简化为一个像素的RGB）
    "label": "橙子",               # 标签
}
```

- 模型学到的，就是"什么样的特征→对应什么标签"
]

=== 2.2 标签只有两类

#key-concept("二分类的标签", [
识物工坊的标签*必须且只能*有"橙子"和"非橙子"两类。
- 不能有第三类（如"不确定"）
- 提交模型时类别名也不能改
- 所以备数据时刻牢记：只分这两类
])

== 三、训练参数：批大小与学习率（20分钟）

=== 3.1 批大小（Batch Size）

#practice[
*操作*：理解批大小，对照训练台的 8/16/32。

```python
# 用一个小例子理解"分批"
dataset = ["图1", "图2", "图3", "图4", "图5", "图6"]
batch_size = 2
for i in range(0, len(dataset), batch_size):
    batch = dataset[i:i+batch_size]
    print("这一批喂给模型：", batch)
```

+ *批大小*：每批喂给模型几张图
+ 识物工坊：8/16/32，默认 16
+ 样本少时*保持 16*；除非多次对比，否则不改
]

=== 3.2 学习率（Learning Rate）

#key-concept("学习率", [
- *学习率*：模型每次"学一步"迈多大。迈太大 → 学不精、乱跳；迈太小 → 学得慢、费时间。
- 识物工坊：0.0005 / 0.001 / 0.003，默认 0.001
- 策略：*先用默认 0.001*；训练不稳定（分数忽高忽低）再小幅调整
- 一次只改一个参数，否则不知哪个起了作用
])

=== 3.3 训练门槛

#warning[
识物工坊每类*至少 5 张*才能开始训练，页面*建议 20 张以上*。比赛训练不要只满足最低门槛——数据越足越多样，模型越稳。这正是前几讲用 Python 备数据的价值。
]

== 四、数据健康度检查（15分钟）

=== 4.1 为什么检查

#practice[
*操作*：提交训练前，先做健康度检查。识物工坊训练台内置此功能。

+ 处理明显*重复*的图（背答案）
+ 处理*模糊*、看不出主体的图（噪声）
+ 处理*类别错误*的图（把橘子误放橙子类会误导）
+ 检查两类数量是否接近
]

=== 4.2 用 Python 辅助检查

#key-concept("辅助健康度检查", [
```python
import os
def health_check(folder):
    files = [f for f in os.listdir(folder) if f.endswith((".jpg", ".png", ".webp"))]
    print(f"【{folder}】共 {len(files)} 张")
    # 可再算重复率、尺寸分布等（结合第3讲）

health_check("orange_train")
health_check("not_train")
```
- 训练前"体检"，比盲目训练再返工高效得多
])

== 五、小结与作业（10分钟）

=== 小结

#key-concept("本节掌握", [
+ 识物工坊 = 二分类（橙子/非橙子）
+ 训练/验证/测试三套数据，测试图绝不进训练
+ 样本（图）、特征（颜色纹理）、标签（答案）
+ 批大小（默认16）、学习率（默认0.001）——先默认，稳定再调
+ 训练前必做数据健康度检查
])

=== 作业

用 Python 统计你的训练集两类各多少张，指出每类是否需要补充；并说明你打算用批大小 16、学习率 0.001 还是调整、为什么。

---

*下讲预告*：训练完怎么知道好不好？下一讲讲模型评估——准确率、混淆矩阵、假阳性/假阴性，以及怎么通过"补难例"把分提上去。
