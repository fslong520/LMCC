#import "../../../../lmcc-theme.typ": *

#show: lmcc-theme.with(
  level: "赛项2·识物工坊 备赛营",
  lesson: "03",
  title: "第3讲 训练二——照方抓药",
  date: "2026年",
)

= 第3讲 训练二——照方抓药

#objective[
- 完整跑通比赛闭环：训练 → 测试 → 误判归档 → 补图 → 重训
- 理解准确率、混淆矩阵，分清假阳性/假阴性
- 掌握"多轮迭代"的节奏：每轮只改一件事，用同一测试集对比
- 体验"正式提交只有一次"的决策压力
]

#warning[
*本讲定位*：*上战场*——第1讲采的图此刻开打。核心玩法是*多轮训练*：
比赛就赢在"训练-测试-归档-补图-重训"这个循环跑几轮、每轮改对地方。
本讲组织为四轮实战，轮间穿插武器教学（混淆矩阵/补难例策略）。
]

== 课前准备（Windows 课堂环境）

- 本讲只用标准库，无需装库
- 课堂脚本：本讲 `课堂脚本/step1` 至 `step5`（评估武器库）；step2 混淆矩阵先手算再对账
- *战场*：比赛平台 `/workshop/`（各组可进）；第1讲各组采样图已就位
- 计分板：每轮自测分、误判归档完整度、最终"一锤定音"提交

== 〇、战况回顾与目标（10分钟）

#key-concept("上讲战报怎么用", [
第一枪的误判清单就是本讲的任务书：
+ *假阳堆*（把非橙认成橙）→ 说明模型见过的"像橙子的非橙"太少
+ *假阴堆*（漏认真橙子）→ 说明橙子样本的变化不够（光线/角度/远近）
本讲目标：*针对两堆各补一轮图，重训，同测试集对比*——看提升幅度。
])

== 一、归档（15分钟）

#practice[
*操作*：用脚本把误判清单整理成两张表（假阳表/假阴表），手算混淆矩阵对账。
]

== 二、照方抓药：补图与重训（30分钟）

#practice[
*操作*：
+ 假阳多 → 补相似非橙子（橘子、橙色球、橙色物）——让模型"见多识广不再上当"
+ 假阴多 → 补不同光线/角度/遮挡的橙子——让模型见过"难认的橙子"
+ 补图后重训（仍默认参数）→ 同一组保留测试图再测
]

#key-concept("对比纪律", [
每一轮都用*同一组*保留测试图——换了测试集，提升作废。记录第二轮自测分。
])

== 三、小结（5分钟）

#key-concept("本讲收获", [
+ 误判是下一轮的弹药清单——比赛就赢在这个循环跑几轮
+ 两类误判，两种补法，别搞反
])

=== 作业

整理你这一轮的误判图，下节课进入训练三：冲刺与定妆。

---

*下讲预告*：分数还差口气？第4讲训练三：最后一轮补刀，然后练"一锤定音"的提交决策。
== 一、准确率与混淆矩阵（20分钟）

=== 1.1 准确率

#practice[
*操作*：理解准确率，并警惕它的局限（数据不平衡时）。

```python
# 用 10 张测试图记录：对了几张
total = 10
correct = 8
print(f"准确率：{correct / total * 100}%")
```

+ *准确率* = 答对 / 总数
+ 若"非橙子"占绝大多数，模型全猜非橙子也能高分——所以要看更细的指标
]

=== 1.2 混淆矩阵

#key-concept("混淆矩阵：看错在哪", [
以橙子为正例：
| | 预测=橙子 | 预测=非橙子 |
|:--|:--|:--|
| 真实=橙子 | ✅ 真阳 | ❌ 假阴（漏认橙子） |
| 真实=非橙子 | ❌ 假阳（把非橙认橙） | ✅ 真阴 |

- *假阳性（F.P.）*：把非橙子认成橙子（如把橘子、橙色球认成橙）
- *假阴性（F.N.）*：没认出橙子（漏掉了真橙子）
- 备赛关注这两类误判，就知道该补什么图
])

== 二、用测试集测真实水平（15分钟）

=== 2.1 记录测试结果

#practice[
*操作*：用识物工坊测试功能，拿一组"从未训练"的新图或摄像头测，记录误判类型。

```python
# 模拟：把测试结果分类，方便后续补难例
results = [
    ("orange", "orange_test_1.jpg", "正确"),   # 真阳
    ("orange", "orange_test_2.jpg", "漏认"),   # 假阴
    ("not_orange", "tangerine.jpg", "误认")    # 假阳（橘子被认成橙）
]
false_positive = [r for r in results if r[2] == "误认"]
false_negative = [r for r in results if r[2] == "漏认"]
print("假阳性(把非橙认橙)：", len(false_positive))
print("假阴性(漏认橙子)：", len(false_negative))
```
]

=== 2.2 为什么必须用保留测试集

#warning[
测试必须用"从未参加训练"的图。用训练过的图测，分数虚高。第3讲划分出的"测试集"就是为这一刻准备的——*只用于测试，绝不训练*。
]

== 三、补难例优化流程（20分钟）

=== 3.1 五步循环

#practice[
*操作*：按"评估→补图→重训→对比→确认"循环优化。

+ ① 把误判图分成两类：假阳性（非橙认橙）、假阴性（漏认橙）
+ ② 补对应难例：
  - 假阳性多 → 增加*相似非橙子*（橘子、橙色球、橙色物）
  - 假阴性多 → 增加*不同光线/角度*的橙子
+ ③ 保持两类数量平衡，重新做健康度检查
+ ④ 重新训练，用同一组保留测试图比较前后结果
+ ⑤ 确认改善，再下载新模型 ZIP，记录版本与日期
]

=== 3.2 补难例要诀

#key-concept("难例怎么选", [
- *假阳性*（把非橙认橙）：找最像橙子的非橙图——橘子、橙子色球、橙色西红柿、橙色包装盒。让模型见多识广，不再上当。
- *假阴性*（漏认橙子）：找模型容易漏的橙子——不同光线（暗处/逆光）、不同角度、远小/近大、背景杂乱、被遮挡的橙子。
- 难例优先于简单重复图：补一张"难图"胜过十张"类似图"。
])

== 四、科学对比：同一测试集（15分钟）

=== 4.1 对照实验

#practice[
*操作*：改数据/参数后，用*同一组*保留测试图对比，避免"换测试集导致分数变化"的假象。

```python
# 用同一测试集比较两个模型版本
testset = ["t1.jpg", "t2.jpg", "t3.jpg", "t4.jpg", "t5.jpg"]
truth   = [1, 0, 1, 1, 0]            # 真实标签：1=橙子，0=非橙子

def accuracy(predictions):
    correct = sum(1 for p, t in zip(predictions, truth) if p == t)
    return correct / len(truth)

v1_pred = [1, 0, 1, 0, 0]   # 模型v1的预测（t4认错）
v2_pred = [1, 0, 1, 1, 0]   # 补难例重训后的模型v2
print("v1 准确率：", accuracy(v1_pred))   # 0.8
print("v2 准确率：", accuracy(v2_pred))   # 1.0
```

+ 同测试集对比，才看得出"这次的改进"是真有效还是碰运气
+ 保留原始训练数据和参数，方便回溯
]

=== 4.2 记录版本

#warning[
每次重训，记录：数据版本、参数、测试集、准确率、误判类型。训练台备份项目（含图片）；导出模型 ZIP 不重命名、不重压缩。这样改进才有据可查。
]

== 五、结算与作业（10分钟）

=== 小结

#key-concept("本节掌握", [
+ 评估：准确率 + 混淆矩阵（假阳/假阴）
+ 测试：必须用"从未训练"的保留测试集
+ 优化循环：误判→分类→补难例→重训→同测试集对比→确认
+ 难例策略：假阳多补相似非橙，假阴多补光线角度橙子
+ 记录版本，备份数据与模型
])

=== 作业

用识物工坊测你的模型，圈出 3 张误判图，标明它们是假阳性还是假阴性，并说明你打算补什么样难例、补几类。

---

*下讲预告（复盘）*：训练台为什么这么强？我们的手写 CNN 为什么打不过它？最后一讲解剖答案——数据量是水，迁移学习是抽水机。
