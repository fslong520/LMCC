# 赛项2 Python 备赛课 · 教师上手指南（Windows 版）

> 写给要用这套《识物工坊 Python 备赛课》（6 讲）的老师。
> 目标：照本指南做，**40 分钟内配好上课环境，每讲任务链能一步一步走完**。
> 讲义在 `lesson.typ`（投影/打印用），课堂脚本在各讲 `课堂脚本/` 目录，逐个运行。

---

## 一、这门课长什么样

- **6 讲**，每讲三件套：讲义 PDF（投屏）+ 课堂脚本（学生跟着跑）+ 作业
- 学生**不写训练代码**——识物工坊在比赛平台的浏览器里训练。Python 的用途是两件：**备好数据**（批量整理图片）+ **弄懂原理**（训练台黑盒里在干什么）
- **引导式上法**：每个脚本开头是一张【任务卡】。三拍节奏——
  1. **抛任务**：只念任务卡，让学生**先猜**"屏幕上会输出什么"
  2. **跑**：学生亲眼看输出，和猜的对账
  3. **改**：按任务卡的"试一试"改一行，重跑看变化
  猜错不是坏事——猜错再验证，印象最深。

## 二、环境配置（任务 0，一次配好）

> 只需做一次。机房已有 Python 的老师可跳到任务 0.4 验证。

### 任务 0.1 安装 Python（10 分钟）

1. 打开官网下载页：https://www.python.org/downloads/windows/
   （打不开就用华为镜像：https://mirrors.huaweicloud.com/python/ ，选 3.12.x 的 `python-3.12.10-amd64.exe`）
2. 双击安装。**第一屏最下面一行"Add python.exe to PATH"必须勾上**——90% 的"python 不是内部命令"都是漏了这一步
3. 点 Install Now，等完成

**验证**：按 `Win+R`，输入 `cmd` 回车，在黑窗口里输入：

```
python --version
```

预期输出：`Python 3.12.x`。

- 显示"不是内部或外部命令"→ 重装并勾选 PATH；或试试 `py --version`（py 启动器装 Python 时会附带）
- 机房还原卡的，装完 Python 后记得提交还原点

### 任务 0.2 给 pip 换国内源（2 分钟）

同在 cmd 里执行（一行）：

```
python -m pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

预期输出：`Writing to ...pip.ini`。不换源下载会慢甚至超时，机房网络差时尤其必要。

### 任务 0.3 安装库（分讲需求）

| 讲次 | 需要的库 | 什么时候装 |
|:----:|:---------|:----------|
| 第 1 讲 | 无（只用标准库） | —— |
| 第 2、3 讲 | pillow | 上第 2 讲前 |
| 第 4、5 讲 | 无 | —— |
| 第 6 讲 | numpy、scikit-learn | 上第 6 讲前 |

嫌分次麻烦，一次装齐（2 分钟）：

```
python -m pip install pillow numpy scikit-learn
```

预期：出现 `Successfully installed ...`。装 sklearn 约 60MB，机房第一次装请耐心。

### 任务 0.4 环境体检（1 分钟）

把整个 `课程-赛项2-Python` 文件夹拷到教师机（U 盘或 git 拉取均可），cmd 里 `cd` 进该目录，运行：

```
python check_env.py
```

预期输出每行都是 `[OK]`（sklearn 一行 `[可装]` 也不影响第 1—5 讲开课）。

### 任务 0.5 认识测试素材（已备好，不用下载）

课程根目录 `测试素材/`：

| 目录 | 类别 | 数量 |
|:-----|:-----|:----:|
| `orange/` | 橙子 | 40 |
| `not_orange/` | 非橙子（**难例**：橘子、杏、南瓜等橙色调近物） | 40 |
| `not_orange_easy/` | 非橙子（对照组：键盘、建筑等明显不像） | 40 |

图片来自 Wikimedia（开放授权，教学用）。**所有脚本会先把素材复制到"我的数据集"工作区再操作，不会动原始素材**——这本身就是第 3 讲"防泄漏、留备份"的身教。

## 三、六讲任务链总表

> 每步 = 一个脚本。`cd` 到对应讲的 `课堂脚本` 目录，逐个 `python 脚本名.py`。
> "学生该发现什么"是本步的探索点——引导学生自己说出来，再翻讲义对答案。

### 第 1 讲 · Python 起步（无需任何库）

| 步 | 脚本 | 预期关键输出 | 学生该发现什么 |
|:--:|:-----|:------------|:--------------|
| 1 | `step1_第一句话.py` | 我的任务是：准备橙子与非橙子图片 | print 与变量：程序在"说话" |
| 2 | `step2_变量与运算.py` | 13.5 / 橙子橙子橙子 / 我有10张橙子图片 | 数字能算、字能拼，f 字符串把变量塞进句子 |
| 3 | `step3_列表与循环.py` | 正在检查： 橙子1.jpg… | 列表是一串东西，for 逐个取 |
| 4 | `step4_字典分类.py` | 橙子类有 2 张… | 字典按"名字"取内容，两类数据各一串 |
| 5 | `step5_判断达标.py` | 刚到最低门槛，建议再加 | if/elif/else 分路走，门槛=每类5张 |
| 6 | `step6_函数封装.py` | 橙子类有20张（假想数） | def 把活封起来，return 把结果交回来 |
| 7 | `step7_遍历文件夹.py` | 发现图片：… 共 40 张橙子图片 | os.listdir 真的数出了素材数量！ |

### 第 2 讲 · 图片与像素（需 pillow）

| 步 | 脚本 | 预期关键输出 | 学生该发现什么 |
|:--:|:-----|:------------|:--------------|
| 1 | `step1_读一张图.py` | 尺寸/格式 + 弹出图片窗口 | 图在电脑里是个对象，有尺寸有格式 |
| 2 | `step2_像素与RGB.py` | (R,G,B) 三个数，红高蓝低 | 橙子像素"红高蓝低"——模型的依据之一 |
| 3 | `step3_颜色规律.py` | 三类图平均RGB对比 | 橙子和橘子平均色接近，难例难就难在这 |
| 4 | `step4_橙色占比.py` | 橙色占比：约 0.6 | 写规则也能"猜"——但只是笨办法 |
| 5 | `step5_批量看尺寸.py` | 每张图名+尺寸列表 | 批量处理=for 循环+库函数 |

### 第 3 讲 · 批量数据准备（需 pillow；核心讲）

| 步 | 脚本 | 预期关键输出 | 学生该发现什么 |
|:--:|:-----|:------------|:--------------|
| 0 | `step0_准备工作区.py` | 复制 16 张图 + 故意埋 2 张重复图 | 动数据先备份工作区，绝不动原始素材 |
| 1 | `step1_统一命名.py` | 已重命名 8 张 | os.rename 批量改名 |
| 2 | `step2_统一尺寸.py` | 8 张全部 128x128 | resize 让数据规整 |
| 3 | `step3_哈希去重.py` | 重复删除：x2 | 指纹相同=同一张图，连拍会"背答案" |
| 4 | `step4_相似度去重.py` | 判定相似/不同的演示 | 近似图缩成 32x32 比像素差 |
| 5 | `step5_划分数据集.py` | 训练 6 张 测试 2 张 | 8:2 划分，测试图绝不进训练 |
| 6 | `step6_健康度检查.py` | 两类张数+是否达标 | 体检先于训练 |

### 第 4 讲 · 机器学习与二分类（无需库）

| 步 | 脚本 | 预期关键输出 | 学生该发现什么 |
|:--:|:-----|:------------|:--------------|
| 1 | `step1_样本特征标签.py` | 一张图的字典表示 | 样本=图、特征=数字、标签=答案 |
| 2 | `step2_分批喂图.py` | 每 2 张一批切出来 | 批大小就是"一口喂几张" |
| 3 | `step3_学习率实验.py` | 三种步长下山对比 | 步太小慢、太大跳过山头——先信默认值 |

### 第 5 讲 · 模型评估与优化（无需库）

| 步 | 脚本 | 预期关键输出 | 学生该发现什么 |
|:--:|:-----|:------------|:--------------|
| 1 | `step1_准确率.py` | 准确率 80.0% | 答对/总数；全猜多数类也能虚高 |
| 2 | `step2_混淆矩阵.py` | 2x2 表格+四格含义 | 错分两种：假阳/假阴，错处不同药方不同 |
| 3 | `step3_误判分类.py` | 假阳 1 假阴 1 | 把误判图归档，才知道补什么 |
| 4 | `step4_同一测试集对比.py` | v1 0.8 → v2 1.0 | 同一组测试图对比，改进才作数 |
| 5 | `step5_补难例策略.py` | 给出"该补什么图"清单 | 评估→补图→重训的循环闭环 |

### 第 6 讲 · 进阶复现（需 numpy + scikit-learn，复用现成脚本）

不另发新脚本，直接用本讲目录下三个现成脚本做**对照实验**（探索式重头戏）：

| 步 | 命令 | 看什么 |
|:--:|:-----|:-------|
| 1 | `python demo.py` | 默认难例数据：准确率、混淆矩阵、样例预测 |
| 2 | `python demo.py ..\..\测试素材\orange ..\..\测试素材\not_orange_easy` | 换简单非橙子：准确率明显升高（约 87%） |
| 3 | `python experiment.py` | 特征x模型全景对比表 |
| 4 | 讨论：同模型同特征，只换数据为什么差 20 个点？ | **数据决定上限**——识物工坊备赛第一原则 |

## 四、常见坑（机房实录）

1. **python 不认识**：装的时候没勾 Add to PATH。重装勾上，或临时用 `py` 代替。
2. **pip 下载超时/SSL 报错**：没换源。回任务 0.2；别在校园网认证页没过的情况下装。
3. **中文乱码**：脚本输出在老 cmd 里乱码时，先执行 `chcp 65001`（切 UTF-8），或改用 Windows Terminal。脚本已尽量避免出问题的字符。
4. **路径**：所有脚本都用相对路径找素材，上课时**先 cd 到脚本所在目录再运行**。整门课的文件夹别改名（"测试素材"这个名字被脚本引用）。
5. **杀毒软件拦 python/pip**：把 Python 安装目录加入信任区。
6. **第 6 讲 sklearn 装不上**（老机器/无网络）：跳过第 6 讲不影响比赛——它只是进阶理解。1—5 讲已覆盖备赛全部刚需。

## 五、和识物工坊比赛的衔接

- 讲义讲"为什么"，平台练"怎么做"。建议：第 3 讲上完进平台做一次真实采样与训练；第 5 讲上完再进平台按"误判→补难例→重训"跑一轮；最后用第 6 讲对照实验收束"数据决定上限"。
- 平台注意（官方手册 V2.0）：浏览器内训练、模型 ZIP ≤ 2MiB、以最后一次提交为准、成绩以后台计算为准。
