#import "../../../../lmcc-theme.typ": *

#show: lmcc-theme.with(
  level: "赛项2·识物工坊 备赛营",
  lesson: "02",
  title: "第2讲 训练一——第一枪",
  date: "2026年",
)

= 第2讲 训练一——第一枪

#objective[
- 进比赛平台完成第一次真实训练（默认参数）
- 记录自测分与每一张误判图
- 理解准确率的含义与局限
- 建立"训练-测试-记录"的第一手体感
]

#warning[
*本讲定位*：四轮训练的*第一轮·摸底*。第1讲你们已用现成素材跑通过流程，这次是*用自己的图*正式打——
本讲只做一件事：完整跑通"上传图片 → 训练 → 测试 → 记录"，
把自测分和误判图全部留下来（第3讲要用）。
像素与原理细节见本讲附篇（`lesson_像素篇` / `lesson_原理篇`），随用随翻。
]

== 课前准备

- 战场：比赛平台 `/workshop/`（各组账号可进）；第1讲各组采样图已导出为图片文件夹
- 课堂脚本：`课堂脚本/`（如需离线演示用）
- 记录单：每组一张"第一枪记录单"——自测分、误判图清单（文件名+错成什么）

== 〇、开打前：检查弹药（10分钟）

#key-concept("上传前的最后体检", [
+ 两类各 ≥20 张？（比赛最低 5 张是底线，20+ 才像样）
+ 测试图已单独放一个文件夹？（*绝不进训练*——防泄漏铁律）
+ 图片命名统一、没有糊图/错类图？（第1讲健康度检查的成果）
有一样不过关，先补课再开枪——比赛中没有"下次再改数据"的从容。
])

== 一、第一枪：训练与测试（30分钟）

#practice[
*操作*：按比赛真实流程走一遍。
+ 上传两类图片到训练台
+ *保持默认参数*开训（批大小16、学习率0.001——迁移学习微调，动它没收益）
+ 用保留测试图逐张测试，记录：自测分 + 误判图清单
+ 每组填"第一枪记录单"，贴到黑板计分区
]

#key-concept("第一枪看什么", [
- 自测分多少不重要，*误判图长什么样*才重要——它们是下一讲的弹药指引
- 记录要用文件名，"好像有几张错的"在比赛中一文不值
- 保留测试集从此封存：四轮训练全用它，才比得出每轮的进步
])

== 二、误判初整理（15分钟）

#practice[
*操作*：把误判图按"把非橙认成橙（假阳）/ 漏认真橙子（假阴）"分成两堆，
贴标签放两个文件夹。下节课的补难例方案就从这两堆里长出来。
]

== 三、小结（5分钟）

#key-concept("本讲收获", [
+ 完整跑通比赛闭环第一遍：上传 → 训练 → 测试 → 记录
+ 自测分与误判清单已留档——这是你战队的"情报"
+ 测试集封存，之后每轮训练都用同一批图对比
])

=== 作业

回家补拍 5 张：专拍你这一组最容易认错的场景（比如逆光的橙子、和橘子放一起的橙子），
下节课带来当补刀弹药。


== 附篇 · 懂原理：训练台里发生了什么



== P.1 分类 vs 回归

#key-concept("二分类是什么", [
- *分类*：判断"是哪一类"。识物工坊判断一张图是"橙子"还是"非橙子"——*二分类*（只有两类）。
- *回归*：预测一个数值（如房价）。这里用不到。
- 识物工坊：输入一张图 → 输出"橙子 / 非橙子"之一。这就是二分类。
])

== P.2 数据集：训练/验证/测试

=== 1.1 三套数据各干什么

#key-concept("三套数据", [
| 数据集 | 用途 | 通俗理解 |
|:----:|:----|:--------|
| 训练集 | 让模型学 | 给模型看题目和答案，让它学规律 |
| 验证集 | 调参时看 | 学的时候小测验，判断学得好不好 |
| 测试集 | 最后检验 | 从未见过的题，考真本事 |

识物工坊训练时内部会分一部分做验证；你单独留的测试图 = 最终的"考试"，绝不进训练。
])

=== 1.2 为什么不能拿测试图训练

#warning[
若把最终测试的图混进训练，模型"见过答案"，自测分会虚高。*测试集必须在训练时完全隔离*——这正是第3讲"8:2划分"防泄漏的原因。
]

== P.3 样本·特征·标签

=== 2.1 三个概念

#practice[
*操作*：理解一张橙子图在模型眼里是什么。

+ *样本*：一张图（一个要判断的对象）
+ *特征*：图里提取出的数字信息（颜色、纹理…）
+ *标签*：答案，橙子=1（或"橙子"），非橙子=0（或"非橙子"）

```python
# 用字典表示一个样本
sample = {
    "path": "orange_1.jpg",       # 样本
    "features": [255, 150, 30],   # 特征（此处简化为一个像素的RGB）
    "label": "橙子",               # 标签
}
```

- 模型学到的，就是"什么样的特征→对应什么标签"
]

=== 2.2 标签只有两类

#key-concept("二分类的标签", [
识物工坊的标签*必须且只能*有"橙子"和"非橙子"两类。
- 不能有第三类（如"不确定"）
- 提交模型时类别名也不能改
- 所以备数据时刻牢记：只分这两类
])

== P.4 训练参数：批大小与学习率

=== 3.1 批大小（Batch Size）

#practice[
*操作*：理解批大小，对照训练台的 8/16/32。

```python
# 用一个小例子理解"分批"
dataset = ["图1", "图2", "图3", "图4", "图5", "图6"]
batch_size = 2
for i in range(0, len(dataset), batch_size):
    batch = dataset[i:i+batch_size]
    print("这一批喂给模型：", batch)
```

+ *批大小*：每批喂给模型几张图
+ 识物工坊：8/16/32，默认 16
+ 样本少时*保持 16*；除非多次对比，否则不改
]

=== 3.2 学习率（Learning Rate）

#key-concept("学习率", [
- *学习率*：模型每次"学一步"迈多大。迈太大 → 学不精、乱跳；迈太小 → 学得慢、费时间。
- 识物工坊：0.0005 / 0.001 / 0.003，默认 0.001
- 策略：*先用默认 0.001*；训练不稳定（分数忽高忽低）再小幅调整
- 一次只改一个参数，否则不知哪个起了作用
])

=== 3.3 训练门槛

#warning[
识物工坊每类*至少 5 张*才能开始训练，页面*建议 20 张以上*。比赛训练不要只满足最低门槛——数据越足越多样，模型越稳。这正是前几讲用 Python 备数据的价值。
]

== P.5 数据健康度检查

=== 4.1 为什么检查

#practice[
*操作*：提交训练前，先做健康度检查。识物工坊训练台内置此功能。

+ 处理明显*重复*的图（背答案）
+ 处理*模糊*、看不出主体的图（噪声）
+ 处理*类别错误*的图（把橘子误放橙子类会误导）
+ 检查两类数量是否接近
]

=== 4.2 用 Python 辅助检查

#key-concept("辅助健康度检查", [
```python
import os
def health_check(folder):
    files = [f for f in os.listdir(folder) if f.endswith((".jpg", ".png", ".webp"))]
    print(f"【{folder}】共 {len(files)} 张")
    # 可再算重复率、尺寸分布等（结合第3讲）

health_check("orange_train")
health_check("not_train")
```
- 训练前"体检"，比盲目训练再返工高效得多
])
