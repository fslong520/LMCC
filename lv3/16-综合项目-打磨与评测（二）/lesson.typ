#import "../../lmcc-theme.typ": *

#show: lmcc-theme.with(
  level: "驾驭AI（创造应用篇）",
  lesson: "16",
  title: "综合项目-打磨与评测（二）",
  date: "2026年",
)

= 综合项目-打磨与评测（二）

#objective[
- 掌握函数封装与代码优化的基本方法，让代码更清晰、更健壮
- 会用 `assert` 与简单单元测试验证函数正确性
- 掌握评测思路：功能测试、边界测试、体验打磨
- 用 AI 辅助做代码审查与优化
]

#warning[
*本节定位：从"能跑"到"好用"。* 上节项目能运行了，本节让它更可靠、更好用。Python 侧的三个关键词——*封装、测试、优化*——是"打磨"的核心。评测不只是"点两下看行不行"，而是系统验证：正常情况、边界情况、错误情况都要测。
]

== 〇、回顾（5分钟）

- LV3-15：项目组装与调试
- 提问："你的项目跑通了吗？哪些输入一进来就会崩？"

== 一、导入（10分钟）

=== 1.1 从"能用"到"好用"

#key-concept("打磨 = 三件事", [
+ *封装*：把重复代码收进函数，一处改、处处变
+ *测试*：用 `assert` 验证"这个输入应该得到那个输出"
+ *优化*：让代码更清晰、更快、更省资源

一条原则：*先跑通，再打磨。* 不要在一开始就追求完美。
])

=== 1.2 为什么测试重要

- 改一个功能，可能弄坏另一个——测试能立刻发现
- 评测 = 给每个功能设"验收标准"，逐项检查

== 二、核心内容（25分钟）

=== 2.1 函数封装：消灭重复

#key-concept("重复代码 → 函数", [
```python
# 不好的写法：重复三次
q1 = input("问题1：")
a1 = ask_ai(q1)
print(a1)
save_record(q1, a1)

q2 = input("问题2：")
a2 = ask_ai(q2)
print(a2)
save_record(q2, a2)

# 好的写法：循环 + 函数
def ask_and_save(question):
    answer = ask_ai(question)
    save_record(question, answer)
    return answer

for _ in range(3):
    q = input("你的问题：")
    ask_and_save(q)
```

- 规律：*看到重复三行，就想封装成函数*
- 封装后：改一处（如加日志），全部生效
])

=== 2.2 assert 断言：最简单测试

#key-concept("assert = 断言", [
```python
def add(a, b):
    return a + b

# 测试
assert add(2, 3) == 5        # 通过，无输出
assert add(-1, 1) == 0
assert add(0, 0) == 0

print("全部测试通过！")
```

- `assert 条件`：条件为假则抛 `AssertionError`，测试失败
- 把"输入 → 期望输出"写成断言，就是最简单的单元测试
])

=== 2.3 边界测试：想全各种情况

#practice[
*边界情况清单*：

+ 空输入：用户直接回车
+ 极值：超大数字、超长文本
+ 非法输入：字母当数字、负数当年龄
+ 外部异常：网络断、文件不存在、API 限流

```python
# 边界测试示例
assert add(2.5, 1.5) == 4.0    # 浮点数
try:
    add("abc", 1)               # 类型错误
    assert False, "应该报错！"
except TypeError:
    print("类型错误已被正确处理")
```
]

=== 2.4 用 AI 做代码审查

#practice[
*AI 结对打磨*：

+ 把完整代码发给 AI："请审查这段代码：1) 找出 bug；2) 指出可优化的地方；3) 提出边界情况"
+ 让 AI 生成测试用例
+ 把 `assert` 断言交给 AI 扩展成更全面的测试
]

== 三、实操练习（30分钟）

=== 3.1 练习一：给项目写测试

#practice[
*任务*：为项目核心函数写 `assert` 测试。

+ 为 `ask_and_save`、`search_local`、`add` 各写至少 3 条断言
+ 覆盖：正常值、边界值、非法输入
+ 运行测试，修复失败项
]

=== 3.2 练习二：体验打磨

#practice[
+ 给程序加"友好提示"：空输入、网络超时、找不到资料时，输出清晰引导而非报错
+ 给 `save_record` 加"防重复"：同一问题多次问，只记一次
+ 挑战：用 AI 辅助把程序改成"多轮对话"（记住前文）
]

== 四、课后作业（10分钟）

1. 完成项目全部 `assert` 测试，记录测试通过率
2. 用 AI 审查代码并采纳至少 3 条优化建议（说明改了什么、为什么）
3. 写一份"评测报告"：功能清单 + 测试结果 + 已知限制

== 五、扩展阅读

- 单元测试入门：https://docs.python.org/zh-cn/3/library/unittest.html
- 下一课预告：LV3-17 项目展示与互评——把你的作品讲给别人听
